apiVersion: v1
kind: Pod
metadata:
  name: frr-bgp2
spec:
  nodeName: helix10.lab.eng.tlv2.redhat.com
  hostNetwork: true
  volumes:
    - name: frr2-config
      configMap:
        name: frr2-config
  containers:
  - name: frr-bgp-pod
    image: frrouting/frr
    volumeMounts:
    - mountPath: "/etc/frr"
      name: frr2-config
    securityContext:
      capabilities:
        add: ["NET_ADMIN","NET_RAW","SYS_ADMIN"]
