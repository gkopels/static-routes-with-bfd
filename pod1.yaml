apiVersion: v1
kind: Pod
metadata:
  name: frr-bgp1
spec:
  nodeName: helix10.lab.eng.tlv2.redhat.com
  volumes:
    - name: frr1-config
      configMap:
        name: frr1-config
  containers:
  - name: frr-bgp-pod
    image: frrouting/frr
    volumeMounts:
    - mountPath: "/etc/frr"
      name: frr1-config
    securityContext:
      capabilities:
        add: ["NET_ADMIN","NET_RAW","SYS_ADMIN"]
